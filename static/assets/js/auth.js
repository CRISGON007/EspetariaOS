document.getElementById('login').addEventListener('click',async()=>{const m=document.getElementById('loginMessage');m.textContent='Entrando...';try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:document.getElementById('username').value,password:document.getElementById('password').value})});const d=await r.json();if(!r.ok)throw new Error(d.detail||'Falha no login.');localStorage.setItem('espetaria_token',d.token);localStorage.setItem('espetaria_user',JSON.stringify(d.user));location.href=d.user.role==='ADMIN'?'/admin':'/atendimento'}catch(e){m.textContent=e.message}});document.getElementById('password').addEventListener('keydown',e=>{if(e.key==='Enter')document.getElementById('login').click()});
